"use strict";

// Public service address only. Administration credentials never ship in the app.
const DEFAULT_RELAY_ORIGIN = "https://vibe-halo-relay.z1593316231.workers.dev";
module.exports = { DEFAULT_RELAY_ORIGIN };
